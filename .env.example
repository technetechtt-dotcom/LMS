# Copy to .env.local for local development (Vite loads VITE_* automatically).

# API base URL (Nest backend)
VITE_API_URL=http://localhost:8787

# Portal: omit or "lms" for main app; "ops" for ops console (vite.ops.config.ts sets this in dev:ops)
# VITE_AUTH_PORTAL=lms
